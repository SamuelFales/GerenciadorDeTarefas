@if (Request.IsAuthenticated)
{
    <text>Usuário: <strong>@User.Identity.Name</strong>!
    [ @Html.ActionLink("Log Off", "LogOff", "User") ]</text>
}
else
{
    @:[ @Html.ActionLink("Log On", "LogOn", "User") ]
}
